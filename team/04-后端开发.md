# 🔧 后端 / 接口开发

## 核心职责

### 1. API 接口设计
- RESTful API 设计规范
- 接口文档编写
- 版本管理策略

### 2. 业务逻辑开发
- 核心业务实现
- 数据流转处理
- 业务规则验证

### 3. 数据库操作
- MongoDB 数据模型设计
- 查询优化
- 索引设计
- 数据迁移

### 4. 安全与鉴权
- 用户认证（JWT）
- 权限控制
- 参数校验
- 安全防护

### 5. 性能优化
- 接口响应优化
- 数据库查询优化
- 缓存策略
- 并发处理

---

## 📋 输出物清单

| 文档/代码 | 说明 | 优先级 |
|-----------|------|--------|
| src/controllers/ | 控制器层 | P0 |
| src/services/ | 服务层 | P0 |
| src/models/ | 数据模型 | P0 |
| src/middleware/ | 中间件 | P0 |
| config/ | 配置文件 | P0 |
| API 文档.md | 接口文档 | P0 |
| 数据库设计.md | 数据模型设计 | P1 |

---

## 🔧 技术栈

```
- 框架：NestJS (v10+)
- 语言：TypeScript
- 数据库：MongoDB
- ODM：Mongoose
- 认证：JWT ( Passport )
- 验证：class-validator, class-transformer
- 缓存：Redis (可选)
- 文档：Swagger / OpenAPI
```

---

## 📁 目录规范

```
backend/
├── src/
│   ├── main.ts                  # 入口文件
│   ├── app.module.ts            # 根模块
│   ├── controllers/             # 控制器
│   │   ├── exhibit.controller.ts
│   │   ├── reservation.controller.ts
│   │   └── user.controller.ts
│   ├── services/                # 服务层
│   │   ├── exhibit.service.ts
│   │   ├── reservation.service.ts
│   │   └── user.service.ts
│   ├── models/                  # 数据模型
│   │   ├── exhibit.schema.ts
│   │   ├── reservation.schema.ts
│   │   └── user.schema.ts
│   ├── dto/                     # 数据传输对象
│   │   ├── create-exhibit.dto.ts
│   │   └── update-exhibit.dto.ts
│   ├── middleware/              # 中间件
│   │   ├── auth.middleware.ts
│   │   └── logger.middleware.ts
│   ├── guards/                  # 守卫
│   │   └── jwt-auth.guard.ts
│   ├── filters/                 # 异常过滤器
│   │   └── http-exception.filter.ts
│   ├── interceptors/            # 拦截器
│   │   └── transform.interceptor.ts
│   └── utils/                   # 工具函数
│       ├── logger.ts
│       └── helpers.ts
├── config/                      # 配置
│   ├── database.config.ts
│   ├── jwt.config.ts
│   └── app.config.ts
├── test/                        # 测试
│   ├── app.e2e-spec.ts
│   └── jest-e2e.json
├── .env                         # 环境变量
├── .env.example                 # 环境变量示例
├── nest-cli.json                # CLI 配置
├── tsconfig.json                # TS 配置
└── package.json                 # 依赖配置
```

---

## 📐 代码规范

### 模块结构
```typescript
// exhibit.module.ts
@Module({
  imports: [MongooseModule.forFeature([{ name: 'Exhibit', schema: ExhibitSchema }])],
  controllers: [ExhibitController],
  providers: [ExhibitService],
  exports: [ExhibitService],
})
export class ExhibitModule {}
```

### 控制器规范
```typescript
// exhibit.controller.ts
@Controller('exhibits')
@ApiTags('展品管理')
export class ExhibitController {
  constructor(private readonly exhibitService: ExhibitService) {}
  
  @Get()
  @ApiOperation({ summary: '获取展品列表' })
  async findAll(@Query() query: QueryDto): Promise<PaginatedResult<Exhibit>> {
    return this.exhibitService.findAll(query);
  }
  
  @Get(':id')
  @ApiOperation({ summary: '获取展品详情' })
  async findOne(@Param('id') id: string): Promise<Exhibit> {
    return this.exhibitService.findOne(id);
  }
  
  @Post()
  @UseGuards(JwtAuthGuard)
  @ApiOperation({ summary: '创建展品' })
  async create(@Body() dto: CreateExhibitDto): Promise<Exhibit> {
    return this.exhibitService.create(dto);
  }
}
```

### 服务层规范
```typescript
// exhibit.service.ts
@Injectable()
export class ExhibitService {
  constructor(
    @InjectModel('Exhibit') private readonly exhibitModel: Model<ExhibitDocument>,
  ) {}
  
  async findAll(query: QueryDto): Promise<PaginatedResult<Exhibit>> {
    const { page = 1, limit = 10, keyword } = query;
    
    const filter: FilterQuery<ExhibitDocument> = {};
    if (keyword) {
      filter.name = { $regex: keyword, $options: 'i' };
    }
    
    const [data, total] = await Promise.all([
      this.exhibitModel.find(filter)
        .skip((page - 1) * limit)
        .limit(limit)
        .exec(),
      this.exhibitModel.countDocuments(filter),
    ]);
    
    return {
      data,
      total,
      page: Number(page),
      limit: Number(limit),
      totalPages: Math.ceil(total / limit),
    };
  }
  
  async findOne(id: string): Promise<Exhibit> {
    const exhibit = await this.exhibitModel.findById(id).exec();
    if (!exhibit) {
      throw new NotFoundException('展品不存在');
    }
    return exhibit;
  }
  
  async create(dto: CreateExhibitDto): Promise<Exhibit> {
    const created = new this.exhibitModel(dto);
    return created.save();
  }
}
```

### 数据模型规范
```typescript
// exhibit.schema.ts
@Schema({ timestamps: true })
export class Exhibit {
  @Prop({ required: true })
  name: string;
  
  @Prop({ required: true })
  description: string;
  
  @Prop()
  imageUrl: string;
  
  @Prop()
  location: string;
  
  @Prop({ type: [String], default: [] })
  tags: string[];
  
  @Prop({ default: true })
  isActive: boolean;
}

export const ExhibitSchema = SchemaFactory.createForClass(Exhibit);

// 索引优化
ExhibitSchema.index({ name: 'text', description: 'text' });
ExhibitSchema.index({ isActive: 1 });
```

### DTO 规范
```typescript
// create-exhibit.dto.ts
export class CreateExhibitDto {
  @IsString()
  @IsNotEmpty()
  @MaxLength(100)
  name: string;
  
  @IsString()
  @IsNotEmpty()
  description: string;
  
  @IsUrl()
  imageUrl: string;
  
  @IsString()
  @MaxLength(50)
  location: string;
  
  @IsArray()
  @IsString({ each: true })
  tags?: string[];
}
```

---

## 🔐 认证与授权

### JWT 配置
```typescript
// jwt.strategy.ts
@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor() {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: process.env.JWT_SECRET,
    });
  }
  
  async validate(payload: JwtPayload) {
    return { userId: payload.sub, username: payload.username };
  }
}
```

### 认证守卫
```typescript
// jwt-auth.guard.ts
@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

---

## 📊 统一响应格式

```typescript
// interceptors/transform.interceptor.ts
@Injectable()
export class TransformInterceptor implements NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    return next.handle().pipe(
      map((data) => ({
        code: 200,
        message: 'success',
        data,
        timestamp: Date.now(),
      })),
    );
  }
}
```

响应格式：
```json
{
  "code": 200,
  "message": "success",
  "data": { },
  "timestamp": 1709481600000
}
```

错误响应：
```json
{
  "code": 400,
  "message": "参数错误",
  "errors": [ ],
  "timestamp": 1709481600000
}
```

---

## 🗄️ 数据库设计

### 集合规划
| 集合名 | 说明 | 主要字段 |
|--------|------|----------|
| users | 用户信息 | _id, username, email, role |
| exhibits | 展品信息 | _id, name, description, imageUrl, location |
| reservations | 预约记录 | _id, userId, exhibitId, date, status |
| visits | 访问记录 | _id, userId, timestamp, duration |

---

## ✅ 开发检查清单

- [ ] API 设计符合 RESTful 规范
- [ ] 参数校验完善
- [ ] 错误处理统一
- [ ] 认证授权实现
- [ ] 数据库索引优化
- [ ] 日志记录完善
- [ ] 单元测试覆盖
- [ ] 接口文档更新

---

## 📞 协作接口

| 协作对象 | 协作内容 |
|----------|----------|
| 架构师 | 技术方案确认、架构设计 |
| 前端开发 | 接口对接、数据格式确认 |
| 客户端开发 | 接口对接、数据格式确认 |
| 测试助手 | 代码审查、问题修复 |

---

**负责人**: AI 后端开发  
**最后更新**: 2026-03-03
