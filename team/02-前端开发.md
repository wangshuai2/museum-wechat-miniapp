# 🎨 前端开发

## 核心职责

### 1. 微信小程序开发
- 页面开发和样式还原
- 组件封装和复用
- 交互逻辑实现
- 性能优化

### 2. Vue 开发
- Vue3 组件开发
- 状态管理实现
- 路由配置
- 响应式设计

### 3. 接口对接
- 与后端 API 对接
- 数据格式转换
- 错误处理
- 请求封装

### 4. 样式与交互
- UI 设计稿还原
- 动画效果实现
- 适配不同屏幕
- 用户体验优化

---

## 📋 输出物清单

| 文档/代码 | 说明 | 优先级 |
|-----------|------|--------|
| pages/ | 小程序页面 | P0 |
| components/ | 公共组件库 | P0 |
| utils/request.js | 请求封装 | P0 |
| utils/helpers.js | 工具函数 | P1 |
| styles/ | 全局样式 | P1 |
| 组件使用文档.md | 组件 API 文档 | P2 |

---

## 🔧 技术栈

### 微信小程序
```
- 基础库版本：>= 2.19.0
- UI 组件库：TDesign / Vant Weapp
- 状态管理：MobX-miniprogram / 原生
- 构建工具：原生 / uni-app
```

### Vue 相关
```
- Vue 版本：Vue 3.x
- 语言：TypeScript
- 构建：Vite
- UI 库：Element Plus / Naive UI
```

---

## 📁 目录规范

```
frontend/
├── pages/
│   ├── index/           # 首页
│   ├── exhibit/         # 展品详情
│   ├── reservation/     # 预约页面
│   └── user/            # 个人中心
├── components/
│   ├── common/          # 通用组件
│   ├── exhibit/         # 展品相关组件
│   └── layout/          # 布局组件
├── utils/
│   ├── request.js       # 请求封装
│   ├── auth.js          # 认证工具
│   └── helpers.js       # 辅助函数
├── styles/
│   ├── variables.wxss   # 变量
│   ├── mixins.wxss      # 混合
│   └── global.wxss      # 全局样式
└── app.js/wxss          # 应用入口
```

---

## 📐 代码规范

### 命名规范
- 文件名：小写 + 连字符（如：exhibit-detail.js）
- 组件名：大驼峰（如：ExhibitCard）
- 变量名：小驼峰（如：exhibitList）
- 常量名：全大写 + 下划线（如：API_BASE_URL）

### 组件规范
```javascript
// 组件结构
Component({
  properties: { },    // 外部属性
  data: { },          // 内部数据
  methods: { },       // 方法
  lifetimes: { }      // 生命周期
})
```

### 样式规范
- 使用 rpx 单位进行适配
- 避免硬编码颜色值
- 使用 CSS 变量管理主题

---

## 🔌 接口对接规范

### 请求封装
```javascript
// utils/request.js
const request = (options) => {
  return new Promise((resolve, reject) => {
    wx.request({
      url: config.baseUrl + options.url,
      method: options.method || 'GET',
      data: options.data,
      header: {
        'Authorization': getToken()
      },
      success: resolve,
      fail: reject
    })
  })
}
```

### 错误处理
- 统一错误提示
- Token 过期自动刷新
- 网络错误友好提示

---

## ✅ 开发检查清单

- [ ] 页面样式与设计稿一致
- [ ] 组件可复用性良好
- [ ] 接口对接完成
- [ ] 错误处理完善
- [ ] 性能优化（图片懒加载、分页等）
- [ ] 兼容性测试通过

---

## 📞 协作接口

| 协作对象 | 协作内容 |
|----------|----------|
| 架构师 | 技术方案确认、规范遵循 |
| 后端开发 | 接口对接、数据格式确认 |
| 测试助手 | 代码审查、问题修复 |
| 客户端开发 | UI 一致性对齐 |

---

**负责人**: AI 前端开发  
**最后更新**: 2026-03-03
