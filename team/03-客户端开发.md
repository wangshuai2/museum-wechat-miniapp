# 📱 客户端开发 (Flutter)

## 核心职责

### 1. Flutter 应用开发
- 页面开发和 UI 实现
- Widget 组件封装
- 交互逻辑实现
- 性能优化

### 2. 跨端适配
- iOS 和 Android 适配
- 不同屏幕尺寸适配
- 平台特性处理

### 3. 接口对接
- 与后端 API 对接
- 数据模型定义
- 网络请求封装
- 错误处理

### 4. 状态管理
- 应用状态设计
- 数据流管理
- 本地持久化

---

## 📋 输出物清单

| 文档/代码 | 说明 | 优先级 |
|-----------|------|--------|
| lib/pages/ | 应用页面 | P0 |
| lib/widgets/ | 公共组件 | P0 |
| lib/models/ | 数据模型 | P0 |
| lib/services/ | 服务层 | P0 |
| lib/utils/ | 工具函数 | P1 |
| test/ | 单元测试 | P2 |

---

## 🔧 技术栈

```yaml
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0          # 网络请求
  provider: ^6.0.0     # 状态管理 (或 bloc/getx)
  shared_preferences:  # 本地存储
  cached_network_image: # 图片缓存
  flutter_localizations: # 国际化
```

---

## 📁 目录规范

```
flutter/
├── lib/
│   ├── main.dart              # 入口文件
│   ├── pages/                 # 页面
│   │   ├── home/              # 首页
│   │   ├── exhibit/           # 展品页
│   │   ├── reservation/       # 预约页
│   │   └── user/              # 用户页
│   ├── widgets/               # 组件
│   │   ├── common/            # 通用组件
│   │   ├── exhibit/           # 展品组件
│   │   └── layout/            # 布局组件
│   ├── models/                # 数据模型
│   │   ├── exhibit.dart
│   │   ├── user.dart
│   │   └── reservation.dart
│   ├── services/              # 服务层
│   │   ├── api_service.dart   # API 服务
│   │   ├── auth_service.dart  # 认证服务
│   │   └── storage_service.dart # 存储服务
│   ├── utils/                 # 工具函数
│   │   ├── constants.dart
│   │   ├── helpers.dart
│   │   └── validators.dart
│   └── routes/                # 路由配置
│       └── app_routes.dart
├── test/                      # 测试文件
│   ├── widget_test.dart
│   └── unit_test.dart
├── assets/                    # 资源文件
│   ├── images/
│   └── fonts/
└── pubspec.yaml               # 依赖配置
```

---

## 📐 代码规范

### 命名规范
- 文件名：小写 + 下划线（如：exhibit_detail.dart）
- 类名：大驼峰（如：ExhibitCard）
- 变量/方法：小驼峰（如：exhibitList）
- 常量：大驼峰 + 静态（如：static const ApiBaseUrl）

### Widget 规范
```dart
// 无状态组件
class ExhibitCard extends StatelessWidget {
  final Exhibit exhibit;
  
  const ExhibitCard({Key? key, required this.exhibit}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children: [
          // UI 实现
        ],
      ),
    );
  }
}

// 有状态组件
class ReservationPage extends StatefulWidget {
  @override
  State<ReservationPage> createState() => _ReservationPageState();
}

class _ReservationPageState extends State<ReservationPage> {
  // 状态管理
}
```

### 状态管理规范 (Provider 示例)
```dart
class ExhibitProvider with ChangeNotifier {
  List<Exhibit> _exhibits = [];
  bool _isLoading = false;
  
  List<Exhibit> get exhibits => _exhibits;
  bool get isLoading => _isLoading;
  
  Future<void> loadExhibits() async {
    _isLoading = true;
    notifyListeners();
    
    try {
      _exhibits = await apiService.getExhibits();
    } finally {
      _isLoading = false;
      notifyListeners();
    }
  }
}
```

---

## 🔌 接口对接规范

### API 服务封装
```dart
// services/api_service.dart
class ApiService {
  final Dio _dio = Dio(BaseOptions(
    baseUrl: Config.baseUrl,
    connectTimeout: 5000,
    receiveTimeout: 3000,
  ));
  
  // 拦截器 - 添加 Token
  ApiService() {
    _dio.interceptors.add(InterceptorsWrapper(
      onRequest: (options, handler) {
        final token = StorageService.getToken();
        if (token != null) {
          options.headers['Authorization'] = 'Bearer $token';
        }
        return handler.next(options);
      },
      onError: (error, handler) {
        if (error.response?.statusCode == 401) {
          // Token 过期处理
        }
        return handler.next(error);
      },
    ));
  }
  
  Future<List<Exhibit>> getExhibits() async {
    final response = await _dio.get('/exhibits');
    return (response.data['data'] as List)
        .map((e) => Exhibit.fromJson(e))
        .toList();
  }
}
```

### 数据模型
```dart
// models/exhibit.dart
class Exhibit {
  final String id;
  final String name;
  final String description;
  final String imageUrl;
  final String location;
  
  Exhibit({
    required this.id,
    required this.name,
    required this.description,
    required this.imageUrl,
    required this.location,
  });
  
  factory Exhibit.fromJson(Map<String, dynamic> json) {
    return Exhibit(
      id: json['_id'],
      name: json['name'],
      description: json['description'],
      imageUrl: json['imageUrl'],
      location: json['location'],
    );
  }
  
  Map<String, dynamic> toJson() {
    return {
      '_id': id,
      'name': name,
      'description': description,
      'imageUrl': imageUrl,
      'location': location,
    };
  }
}
```

---

## ✅ 开发检查清单

- [ ] UI 与设计稿一致
- [ ] 组件可复用性良好
- [ ] 状态管理合理
- [ ] 接口对接完成
- [ ] 错误处理完善
- [ ] 性能优化（图片缓存、列表分页等）
- [ ] 跨端兼容性测试通过
- [ ] 单元测试覆盖关键逻辑

---

## 📞 协作接口

| 协作对象 | 协作内容 |
|----------|----------|
| 架构师 | 技术方案确认、架构设计 |
| 前端开发 | UI 一致性对齐、组件设计 |
| 后端开发 | 接口对接、数据格式确认 |
| 测试助手 | 代码审查、问题修复 |

---

**负责人**: AI 客户端开发  
**最后更新**: 2026-03-03
